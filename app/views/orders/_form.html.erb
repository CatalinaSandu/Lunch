<%= form_for(@order) do |f| %>
<% if @order.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

  <ul>
    <% @order.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= f.label :menu_id %><br>
  <%=  f.select( :menu_id, Menu.where("DATE(date) = ?", Date.today).map {|a| [a.title, a.id]} ,  {prompt: "Please Choose"}) %>
</div>
<% if current_user.admin? %>
<div class="field">
  <%= f.label :order_status %><br>
  <%= f.text_field :order_status %>
</div>
<% end %>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>
